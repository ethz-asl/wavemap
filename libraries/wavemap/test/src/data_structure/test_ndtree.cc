#include <gtest/gtest.h>

#include "wavemap/data_structure/ndtree/ndtree.h"
#include "wavemap/test/fixture_base.h"

namespace wavemap {
using NdtreeTest = FixtureBase;

// TODO(victorr): Test the NdTree data structure directly (not only indirectly
//                through the volumetric data structure tests).
TEST_F(NdtreeTest, DummyTest) {}
}  // namespace wavemap
