import numpy as np

# Load a map
# from io_load_map_from_file import your_map

# Declare the point to query [in map frame]
query_point = np.array([0.4, .5, 0.6])

# TODO(victorr): Extend the bindings
# TODO(victorr): Include vectorized version
occupancy_log_odds = 0.0  # placeholder
